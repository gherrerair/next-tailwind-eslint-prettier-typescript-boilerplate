const Layout: React.FC = () => {
  return (
    <>
      <p className="text-8xl">Layout Component!</p>
      <button onClick={() => window.alert('Hello Layout!')}>Click me</button>
    </>
  );
};

export default Layout;
